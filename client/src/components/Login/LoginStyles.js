import { StyleSheet } from 'aphrodite';

const styles = StyleSheet.create({
    LoginContainer: {
        width: '92%',
        top: '-170px',
        position: 'relative',
    },
});

export default styles;